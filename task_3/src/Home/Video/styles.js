import styled from 'styled-components';

const Tile = styled.div`
  background: #fafafa;
  border: 1px solid #777;
  padding: 0 20px;
`;

const VideoContainer = styled.div``;

const VideoTitle = styled.h4`
  margin: 10px;
`;

export { Tile, VideoContainer, VideoTitle };
